{
  "name": "dsh-feishu-reader",
  "version": "1.1.0",
  "description": "DSH Cordis 插件：读取飞书文档（docx/wiki/sheets/bitable），文字转 Markdown，表格还原、图片下载到本地临时缓存供 AI 读图。",
  "main": "lib/index.js",
  "scripts": {
    "test": "node --test lib/script.test.js",
    "check": "node --check lib/script.js && node --check lib/index.js && node --check lib/client.js && node --check host.js && node --check client.js",
    "build:dynamic": "node scripts/build-dynamic.mjs"
  },
  "keywords": [
    "dsh",
    "cordis",
    "feishu",
    "lark",
    "飞书",
    "document",
    "docx",
    "wiki",
    "markdown"
  ],
  "license": "MIT",
  "engines": {
    "node": ">=18"
  }
}
